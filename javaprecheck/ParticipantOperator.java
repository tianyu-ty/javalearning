package javaprecheck;

import java.util.ArrayList;

public interface ParticipantOperator {

	 void sportType(ArrayList<Participant> participants);
	 void awardsNumber(ArrayList<Participant> participants);
}
